## Solution
```
// The following function was supposed to be the constructor but there was a typo, so the owner has not been set yet
// We'd be the owner once we call the function successfully
await contract.Fal1out()
```